# 项目开发约定

<!-- 供人工复制的模板，不是 ai-team 工作流指令。
可复制到目标项目根目录并命名为 AGENTS.md；也可复制为
docs/<游戏或模块>/project-rules.md，在任务中显式指定
project_rules_file: docs/<游戏或模块>/project-rules.md，无需创建根目录 AGENTS.md。
填写所有 <待填写> 项，删除不适用的内容，再开始使用。
如果目标项目已有 AGENTS.md，手动合并，不要直接覆盖。
角色、模型和通用流程由 ai-team 提供，这里只写项目自身约定。 -->

## 项目说明

- 项目用途：<待填写：项目解决的问题及主要功能>
- 本次项目范围：<待填写：单仓库或涉及的其他仓库及职责>
- 适用游戏或业务模块：<待填写：本规则覆盖的范围，不涉及的游戏不加载其规则>
- 公共模块规则来源：<待填写：触及共享代码时需要读取的规则路径，不涉及则删除>

## 开发角色选择

保留下面一项，删除另外两项。只选择本项目负责实现的端：

```text
development_roles: [backend]
development_roles: [client]
development_roles: [backend, client]
```

这是 ai-team 读取的项目指令约定，不是 Codex config.toml 的配置项。
product、architect、planner、reviewer、qa 和主会话编排保持不变。
未选中的开发角色不调用，也不等待其产物；单端项目照常完成完整工作流。
涉及另一端的现有接口时，说明接口约定和验证范围；真正需要修改另一端时，
先明确项目范围或调整角色选择，不能直接忽略该需求。

## 技术栈

- 语言与版本：<待填写：例如 Go，版本以 go.mod 为准>
- 框架与依赖：<待填写：沿用的框架、数据库等>
- 优先沿用现有框架、依赖和代码组织方式。

## 目录约定

本文件声明的项目相对路径均以 project_root（目标仓库根目录）为基准，
不是本规则文件所在目录。按实际目录填写，不要为了匹配模板重组代码。

- 后端代码：<待填写>
- 客户端或前端代码：<待填写，不涉及则删除>
- 协议定义：<待填写，不涉及则删除>
- 公共组件：<待填写>
- 测试与测试数据：<待填写>

## 业务规则与设计依据

- 需求和业务规则来源：<待填写：文档路径或其他明确来源>
- 架构文档与 ADR：<待填写：现有路径>
- 业务 skill 及触发条件：<待填写：涉及哪些任务时必须读取哪个 SKILL.md，不涉及则删除>
- 先读取适用 skill，再按其导航读取相关文档；只执行当前角色权限与阶段允许的步骤。
- 本规则补充项目事实，不替代 ai-team 流程或屏蔽其他适用指令；冲突应报告并评审。
- 未明确的业务规则应提出问题，不自行补造。

## 文档与产物

优先沿用项目现有文档目录。下列为默认位置，可按项目实际情况修改：

- 需求文档：docs/product/
- 架构文档：docs/architecture/
- 任务计划、审查、QA 和进度记录：docs/tasks/<task-id>/
- 修改已有功能时优先更新原有文档，避免重复创建。
- 项目代码、文档和测试产物保存在本项目内。

## 开发与验证

- 环境准备：<待填写：安装依赖、启动依赖服务等步骤>
- 构建命令及工作目录：<待填写>
- 受影响模块的测试命令及工作目录：<待填写>
- 交付前必需检查：<待填写>
- 并发、集成或端到端检查的触发条件：<待填写>
- 检查无法执行时记录原因及未验证范围，不标记为通过。

<!-- Go 项目可参考以下示例，确认适用后填入上面的约定：
在对应 Go 模块根目录执行命令。
开发时：go test ./实际受影响的包/...
交付前：go test ./...
涉及并发修改时：go test -race ./实际受影响的包/...
多模块项目逐一列出各模块的工作目录和检查命令。 -->

## 修改边界

- 生成文件及生成命令：<待填写：哪些文件不能手改，如何重新生成>
- 受保护目录或外部子模块：<待填写>
- 兼容性要求：<待填写：接口、协议、数据库等约束>
- 保留已有的无关修改，不覆盖其他任务的工作。
- 不通过删除或绕过失败测试来完成验收。
