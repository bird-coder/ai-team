# Unit-test acceptance: compatibility entry

The full standard now lives in the shared skill
[ai-team-unit-tests](../skills/ai-team-unit-tests/SKILL.md).

Before planning, implementing or reviewing unit-test acceptance, read that SKILL.md
and its linked acceptance standard completely. This file is only a routing entry,
not a substitute for the standard. Existing callers may keep this path; the rules,
role ownership, phase gates and verdict semantics have not changed.
